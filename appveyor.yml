os: Visual Studio 2015

platform:
  - x64

configuration:
  - Debug
  - Release

matrix:
  fast_finish: true

shallow_clone: true

clone_folder: c:\projects\libchen

before_build:
  - pwd
  - cmake --version
  - cd test
  - mkdir build
  - cd build
  - cmake ..
  - ls

build:
  project: C:\projects\libchen\test\build\test_libchen.sln

test_script:
  - cmd: pwd
  - cmd: Debug\test_libchen.exe
  - cmd: Release\test_libchen.exe