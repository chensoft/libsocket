language: cpp

os:
  - linux
  - osx

compiler:
  - gcc
  - clang

sudo: required
dist: trusty

script:
  - cd test
  - mkdir build
  - cd build
  - cmake ..
  - make
  - ./test_libchen --data=../data

notifications:
  email:
    on_success: never
    on_failure: always