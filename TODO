-) rename test folder to unit

-) add demo folder to add some example usage in it

-) add c++ test framework to do unit test

-) json decode just store pointer, don't copy string if not needed

-) the capability of sending raw packet